SELECT * FROM USUARIO WHERE Reestablecer = 1; -- Devuelve todos los usuarios que tienen Reestablecer en 1

SELECT Nombres FROM USUARIO WHERE Reestablecer = 1; -- Devuelve los nombres de los usuarios que tienen Reestablecer en 1

SELECT DISTINCT Reestablecer FROM USUARIO WHERE Reestablecer = 1; -- Devuelve los valores distintos de Reestablecer que son 1
